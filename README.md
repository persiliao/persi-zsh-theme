# Persi Zsh Theme

# Install 

```bash
$ cd $ZSH_CUSTOM/themes
$ git clone https://github.com/persiliao/persi-zsh-theme.git
$ cp -R persi-zsh-theme/* .
$ vim ~/.zshrc
$ ZSH_THEME='persi'
$ source ~/.zshrc
```